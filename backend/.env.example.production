# Backend Production Environment Variables
# These will be configured in Railway dashboard

# Database (Railway PostgreSQL plugin provides this automatically)
DATABASE_URL=postgresql://user:password@host:5432/railway

# Redis (Railway Redis plugin provides this automatically)
REDIS_URL=redis://default:password@host:6379

# JWT Configuration (GENERATE NEW SECRETS FOR PRODUCTION!)
# Use: openssl rand -base64 32
JWT_SECRET=your-production-jwt-secret-min-32-chars
JWT_REFRESH_SECRET=your-production-refresh-secret-min-32-chars
JWT_ACCESS_DURATION=15m
JWT_REFRESH_DURATION=168h

# Server Configuration
SERVER_PORT=8080
ENVIRONMENT=production

# CORS (Update with your frontend Railway URL)
CORS_ORIGINS=https://your-frontend.up.railway.app

# Rate Limiting
LOGIN_RATE_LIMIT=5

# Health Monitoring
HEALTH_CHECK_INTERVAL=60s
ALERT_COOLDOWN_MINUTES=30

# Optional: Email notifications (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@vitalconnect.com.br
# ADMIN_ALERT_EMAIL=admin@vitalconnect.com.br

# Optional: Push notifications (FCM)
# FCM_SERVER_KEY=your-firebase-server-key
